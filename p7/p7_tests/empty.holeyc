int result;

int main(){
  int x;
  char b; 
  b = '1;
  x = 2; 
  if(true && true){
    TOCONSOLE "PASS: Inside true && false if block.\n";
  }
  if(false || true){
    TOCONSOLE "PASS: Inside false || false if block.\n";
  }
  TOCONSOLE b;
}


#  SayHello: pushq %rbp 
#            movq %rsp, %rbp
#            addq $16, %rbp 
#            subq $16, %rsp <-- allocate enough space for fn locals 
#            movq $0, %rax
#            movq %rax, (result's location)
#            addq $16, %rsp <-- depends on subq above. 
#            popq %rbp
#            retq