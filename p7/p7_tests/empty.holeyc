int result;

int giveMeFive(){
  result = 5;
  # still need AND to work 
  if(true && false){
    TOCONSOLE "ASSERT: [AND FAILS].\n";
  }
  return 5; 
}

int main(){
  int x;
  if(true){
    x = giveMeFive();
    TOCONSOLE x+4;
  } else {
    TOCONSOLE "FART\n";
  }  
}


#  SayHello: pushq %rbp 
#            movq %rsp, %rbp
#            addq $16, %rbp 
#            subq $16, %rsp <-- allocate enough space for fn locals 
#            movq $0, %rax
#            movq %rax, (result's location)
#            addq $16, %rsp <-- depends on subq above. 
#            popq %rbp
#            retq